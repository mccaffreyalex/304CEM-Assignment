'use strict'
const flickr = require('../modules/flickr')
const address = 'London'
describe('Searching Location', () => {
    it('should put the locaion into the Flickr API URL', () => {});
    flickr.add('London')
    expect()
});
